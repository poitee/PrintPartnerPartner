# Example plan kit overlay fragment (v2 export / profile kit-manifest).
# Generated by the Kit Configurator; paste into Plan → Advanced only when debugging.
kit:
  selections:
    toolhead: stealthburner
    probe: voron_tap
    z_drives: stock
  choice_tree:
    - id: z_drives
      label: Addon Z drives
    - id: toolhead
      label: Addon Toolhead
    - id: probe
      label: Addon Probe
  replacements: {}
  # replacements: { "addons/voron-stealthburner/.../hull.stl": "base/.../stock_hull.stl" }
