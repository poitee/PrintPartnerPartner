# Klicky-Probe — Print Partner workflow
1. Add `Klicky-Probe`; **Sync** (`main`, no tags).
2. Attach as **addon (probe)**; scope = matching printer folder + one Probes/ flavor.
3. Manifest: probe body + printer mount/dock set + note Klipper_macros as config (not print) items.
4. On Micron bases: take dock parts from the Micron repo instead.
5. Deselect Tap/stock-probe alternatives; recompute. **Sync after ref change.**
