# LDOVoron0 — Print Partner workflow
1. Add `LDOVoron0`; **Sync**. Pick BRANCH matching the kit: `v02r1` (current), `v02`, or `v01`. **Sync again after switching branch.**
2. Attach as **addon** on `Voron-0@Voron0.2r1` base.
3. Selections: Power_Inlet (yes for LDO inlet), Mornsun PSU cover (recommended), chain-anchor side skirt vs stock skirt, Picobilical toolhead parts, nameplate/spool-holder extras.
4. Preset_Print_PlatesR1 are convenience plates — exclude from per-part manifests to avoid double counting.
5. Recompute after toggling Picobilical.
