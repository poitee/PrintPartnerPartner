# Voron-Legacy — Print Partner workflow
1. Add `Voron-Legacy`; **Sync**. Ref `main` or tag `V1.0`.
2. Attach as **base**; optional addons: Klicky (Legacy scope), SB toolhead.
3. Quote/escape folder names with spaces when building the manifest.
4. **Sync after changing tag.**
