# voron_0_pancake_board — Print Partner workflow
1. Add `voron_0_pancake_board`; **Sync** (`master`, no tags).
2. Attach as **addon (electronics)** on a Voron-0 stack — V0.1/MiniAB era; flag UNKNOWN for V0.2 builds.
3. Manifest: stl/backingplate.stl + stl/cover.stl only (gerbers are fab files).
4. **Sync after ref change.**
