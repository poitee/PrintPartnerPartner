# Boop — Print Partner workflow
1. Add `Boop`; **Sync** (`main`, no tags — pin commit).
2. Attach as **addon (probe)**; scope to `beta_4/STL` (current revision).
3. Selections: pcb vs wired upper, 3mm vs 4mm belt spacing center.
4. Deselect competing probe/carriage parts; recompute. **Sync after ref change.**
