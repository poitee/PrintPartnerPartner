# Voron-Extras — Print Partner workflow
1. Add `Voron-Extras`; **Sync** (`main` only, no tags).
2. Attach as **addon (accents)**; scope to a subfolder (e.g. Fidget_Toy/STLs).
3. Exclude Bed_Models (slicer assets) and Font from print manifests.
4. **Sync after changing ref.**
