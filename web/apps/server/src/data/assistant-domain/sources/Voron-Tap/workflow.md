# Voron-Tap — Print Partner workflow
1. Add `Voron-Tap`; **Sync**. Only ref is `main` (no tags) — pin by commit if reproducibility matters.
2. Attach as **addon (probe)** on V2 or Trident base only.
3. Manifest selections: upper variant (PCB / D2HW / Wired), center left+right (+ [a] center), belt covers x2.
4. Mark replaced base parts (carriage, z endstop/nozzle probe) as deselected; recompute.
5. **Sync after any ref change**; watch Manual/R8_errata.md for revision corrections.
