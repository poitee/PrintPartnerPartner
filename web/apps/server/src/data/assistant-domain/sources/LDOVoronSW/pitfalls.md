# LDOVoronSW — pitfalls
- Requesting branch `main`/`master` — only `s1` exists as default.
- Grills exist BOTH upstream (STL/Grills) and here (STLs/Grills) — pick one set.
- Note upstream root is STL/ but this repo uses STLs/ — path logic must handle both.
