# chirpy2605-voron — Print Partner workflow
1. Add `chirpy2605-voron`; **Sync** (`main`, no tags — pin commit).
2. Attach as **addon**, scoped to ONE mod folder (e.g. V0/Dragon_Burner) plus the matching mount from general/Alternative_Voron_Mounts when on Trident/2.4/Legacy/SW.
3. Deselect the stock toolhead parts it replaces; recompute.
4. **Sync after ref change.**
