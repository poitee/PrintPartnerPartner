# LDO-Milo-V1.5 — Print Partner workflow
1. Add `LDO-Milo-V1.5`; **Sync** (`master`, no tags).
2. This is a CNC mill kit supplement — keep it out of printer stacks; treat as its own base-family.
3. Use STLs/PRINTED_PARTS.md as the manifest ground truth; filenames contain spaces (and double spaces) — quote paths.
4. **Sync after ref change.**
