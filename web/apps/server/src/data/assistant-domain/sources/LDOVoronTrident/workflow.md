# LDOVoronTrident — Print Partner workflow
1. Add `LDOVoronTrident`; **Sync**. Default branch `master` (no tags).
2. Attach as **addon** on `Voron-Trident@VTr2` base + SB toolhead.
3. Selections: power inlet 1.2mm (deselect Trident rear_skirt_power_adamstech), LDO deck wire cover right (deselect wire_cover_right), z cable anchor, bed WAGO mount, optional TFT mount / angled exhaust cover.
4. Recompute after switching Trident tag (VTr2 <-> main) — panel clips change upstream.
5. **Sync after changing tag/branch.**
